general {
    # The lock screen is quickshell's (dot_config/quickshell/Lock.qml). Locking
    # is idempotent there, so no `pidof` guard is needed. hypridle stays because
    # quickshell has no logind binding: this is what holds the sleep inhibitor.
    lock_cmd = qs ipc call lock lock
    before_sleep_cmd = qs ipc call lock lock && sleep 0.5  # lock before suspend; the sleep lets the lock surface commit.
    after_sleep_cmd = hyprctl dispatch dpms on             # to avoid having to press a key twice to turn on the display.
}

listener {
    timeout = 300                         # 5min.
    on-timeout = brightnessctl -s set 10  # set monitor backlight to minimum, avoid 0 on OLED monitor.
    on-resume = brightnessctl -r          # monitor backlight restore.
}

# turn off keyboard backlight, comment out this section if you dont have a keyboard backlight.
# listener {
#     timeout = 300                                           # 5min.
#     on-timeout = brightnessctl -sd rgb:kbd_backlight set 0  # turn off keyboard backlight.
#     on-resume = brightnessctl -rd rgb:kbd_backlight         # turn on keyboard backlight.
# }

listener {
    timeout = 360                        # 6min
    on-timeout = qs ipc call lock lock   # lock screen when timeout has passed
}

listener {
    timeout = 420                                              # 7min
    on-timeout = hyprctl dispatch dpms off                     # screen off when timeout has passed
    on-resume = hyprctl dispatch dpms on && brightnessctl -r   # screen on when activity is detected after timeout has fired.
}

listener {
    timeout = 600                    # 10min
    on-timeout = systemctl suspend   # suspend pc
}

